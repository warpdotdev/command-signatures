{
  "name": "events",
  "description": "Receive real time events from containers",
  "args": {
    "generators": {
      "script": "_NuFrRa_e => {\n    if (e.includes(\"-f\")) {\n      let s = e.indexOf(\"-f\");\n      return \"docker-compose -f \".concat(e[s + 1], \" config --services\");\n    }\n\n    return \"docker-compose config --services\";\n  }",
      "splitOn": "\n"
    }
  },
  "options": [
    {
      "name": "--json",
      "description": "Output events as a stream of json objects"
    }
  ]
}
