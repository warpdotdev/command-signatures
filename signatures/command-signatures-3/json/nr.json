{
  "name": "nr",
  "description": "Use the right package manager - run",
  "options": [
    { "name": ["-h", "--help"], "description": "Output usage information" }
  ],
  "args": {
    "name": "script",
    "description": "The script to run",
    "generators": {
      "script": "until [[ -f package.json ]] || [[ $PWD = '/' ]]; do cd ..; done; cat package.json",
      "postProcess": "function (e, _ref) {\n    let [n] = _ref;\n    if (e.trim() == \"\") return [];\n\n    try {\n      let a = JSON.parse(e),\n          t = a.scripts,\n          o = a.fig || {};\n      if (t) return Object.entries(t).map(_ref2 => {\n        let [c, p] = _ref2;\n        let s = n === \"yarn\" ? \"fig://icon?type=yarn\" : \"fig://icon?type=npm\",\n            m = o[c];\n        return _objectSpread({\n          name: c,\n          icon: s,\n          description: p\n        }, m);\n      });\n    } catch (a) {\n      console.error(a);\n    }\n\n    return [];\n  }"
    }
  },
  "additionalSuggestions": [
    {
      "name": "-",
      "insertValue": "-\n",
      "description": "Run the last command",
      "type": "shortcut"
    }
  ]
}
